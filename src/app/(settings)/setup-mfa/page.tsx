import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Setup MFA',
};

export default function SetupMfaPage() {
  return (
    <main>
      <h1>Setup MFA</h1>
    </main>
  );
}
