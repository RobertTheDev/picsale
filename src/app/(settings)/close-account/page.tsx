import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Close Account',
};

export default function CloseAccountPage() {
  return (
    <main>
      <h1>Close Account</h1>
    </main>
  );
}
