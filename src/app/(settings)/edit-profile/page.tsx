import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Edit Profile',
};

export default function EditProfilePage() {
  return (
    <main>
      <h1>Edit Profile</h1>
    </main>
  );
}
