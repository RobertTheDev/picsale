import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Listings',
};

export default function ListingsPage() {
  return (
    <main>
      <h1>Listings</h1>
    </main>
  );
}
